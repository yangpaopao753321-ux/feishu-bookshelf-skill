---
name: feishu-bookshelf
description: 把书籍自动录入到用户自己的飞书多维表格。当用户说"记本书"、"加本书"、"我想读《XXX》"、"我刚看完了《XXX》"、"开始读《XXX》了"、"把XXX加入书单"、"标记读完XXX"、"书籍入库"等任何往读书清单/书架加书或更新阅读状态的请求时，必须使用此 Skill。也用于用户口述对某本书的读后感/想法/笔记时，把想法整理后写入该记录的笔记字段。首次使用（local-config 不存在）时引导用户完成表链接配置。
---

# Feishu Bookshelf — 飞书书籍表自动录入管线

把用户提到的书写入用户自己的飞书多维表格。这是一个通用管线：不绑定任何特定表，首次使用时引导用户配置自己的表。

## 前置依赖

- 运行环境有 `lark-cli` 命令行工具，且已完成飞书 OAuth 登录（`lark-cli auth status` 能看到已登录用户）
- 如果环境没有 lark-cli，无法使用本 Skill

## 第 0 步：加载本地配置（每次必做）

先读 `references/local-config.md`。

- **如果文件不存在**：说明是首次使用。按 [references/init-guide.md](references/init-guide.md) 引导用户完成初始化（拿表链接 → 确认编辑权限 → 自动读字段并确认映射 → 保存配置），完成后再继续。
- **如果文件存在**：从中读取 `base_token`、`table_id`、字段映射（语义角色→实际字段名）、分类选项列表、状态选项列表。后续所有命令用这些值，不要重新 url-resolve。

## 工作流程

**重要：下面提到的"书名""作者""分类"等都是语义角色，实际写入时用 local-config.md 里记录的用户真实字段名。** 比如用户表里字段叫"Book Title"，写入 JSON 里就用"Book Title"。

### 1. 判断阅读状态

从用户原话判断本次记录属于哪种状态语义：

| 用户说法 | 状态语义 | 需要补全的字段 |
|---|---|---|
| "想读 / 加一本 / 记本书 / 加入书单 / 待读" | 待读 | 书名 + 日期(今天) |
| "开始读 / 正在读 / 翻到一半了" | 在读 | 书名 + 日期(今天) |
| "看完了 / 读完了 / 刚读完 / 标记已读 / 收尾了" | 已读 | 全部字段，需联网搜索补全 |

用户没明说时，默认按"待读"处理。

**写入时的状态选项名必须从 local-config.md 的"状态选项"表里取**，不要自己写死"待阅 🎯"——不同用户表里的选项名可能完全不同（有的叫"待读"，有的叫"想读"，有的带 emoji）。

### 2. 提取书名

- 用户给的书名可能带也可能不带《》。写入时统一补成《XXX》格式。
- 中英文书名都支持。

### 3. 补全书籍信息（仅"已读"场景）

状态语义为"已读"时，用 general_search 搜索书籍，补全以下字段：

- **作者**：作者原名，保留中文译名规范
- **分类**：从 local-config.md 的已有选项里选 1-2 个最贴切的；不要自创选项
- **出版日期**：原版首次出版日期，格式 `YYYY-MM-DD 00:00`
- **简介**：30-100 字的内容总结，聚焦这本书讲了什么核心观点，不要写空泛评语

状态语义为"待读"或"在读"时，跳过搜索，只写书名 + 日期 + 状态。

### 4. 预览与写入

- **待读 / 在读**（只填了书名）：直接写入，不用预览确认。
- **已读**（搜索补全了信息）：先把拟写入的字段列成表格给用户看一眼，说"确认写入？"，用户确认后再调 lark-cli。

写入命令（`--json`，外层包 `create_records` 数组，每条记录直接是字段映射，不要嵌套 `fields`；BASE_TOKEN、TABLE_ID、字段名、选项名都从 local-config.md 读）：

**简单记录（少字段、无长文本）可直接内联：**

```bash
lark-cli base +record-batch-create \
  --base-token "<BASE_TOKEN>" \
  --table-id "<TABLE_ID>" \
  --json '{"create_records":[{"<书名字段>":"《书名》","<日期字段>":"2026-09-24 00:00","<状态字段>":["<待读对应的实际选项名>"]}]}' \
  --as user
```

**复杂记录（含简介长文本、多个数组字段）必须用 Python 生成临时 JSON 文件再传，避免 shell 转义出错：**

```python
import json
data = {"create_records": [{"<书名字段>": "《书名》", "<作者字段>": "...", "<分类字段>": ["..."], "<出版日期字段>": "YYYY-MM-DD 00:00", "<简介字段>": "...", "<日期字段>": "YYYY-MM-DD 00:00", "<状态字段>": ["<已读对应的实际选项名>"]}]}
with open('/tmp/bookshelf_record.json', 'w', encoding='utf-8') as f:
    json.dump(data, f, ensure_ascii=False)
```

然后：
```bash
lark-cli base +record-batch-create \
  --base-token "<BASE_TOKEN>" \
  --table-id "<TABLE_ID>" \
  --json @/tmp/bookshelf_record.json \
  --as user
```

### 5. 回读验证

写入成功后，回读最新一条记录确认字段值正确，再告诉用户"已录入 ✅"并简列写入内容。

### 6. 写读书笔记（用户口述想法时）

当用户对某本已录入的书说"我觉得这本书..."、"这本书给我的启发是..."、"笔记：..."等想法时：

1. 先用 `+record-search` 按书名定位到 record_id
2. 把用户的口语想法整理成通顺的笔记（保留原意，不要过度润色或添加书中内容）
3. 用 `+record-batch-update` 更新该记录的笔记字段
4. 如果该记录已有笔记，追加在后面（用换行分隔），不要覆盖

```bash
lark-cli base +record-batch-update \
  --base-token "<BASE_TOKEN>" \
  --table-id "<TABLE_ID>" \
  --json '{"update_records":[{"record_id":"recXXX","fields":{"<笔记字段>":"整理后的想法..."}}]}' \
  --as user
```

## 字段写入规则速查

- 日期字段传 `"YYYY-MM-DD HH:mm"` 字符串（这是 API 格式，和用户表里的显示格式无关；读完/加入日期统一用 `00:00`）
- 分类字段传字符串数组，如 `["心理学", "AI"]`
- 状态字段传单元素数组，值从 local-config.md 读
- text 字段直接传字符串
- select 选项名必须与 local-config.md 里记录的精确匹配（包括空格、emoji、大小写）
