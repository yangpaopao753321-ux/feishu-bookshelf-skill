# Feishu Bookshelf 使用手册

在豆包（或其他支持 skill 的 agent）里说一句话，就能把书录进你的飞书多维表格。

---

## 你需要准备什么

1. **飞书账号**，并且已经在飞书里建好了一张多维表格用来存书
2. **agent 环境已装好 lark-cli 并完成飞书登录**（这是 agent 侧的依赖，不是你本地要装的；如果初始化时 agent 提示没装 lark-cli 或没登录，它会告诉你怎么处理）
3. 表格链接

---

## 两条路径

### 路径 A：我已经有表了（推荐）

直接把你的多维表格链接发给 agent，说一句：

> "帮我打通飞书书架，这是我的表：https://xxx.feishu.cn/base/xxxxx"

agent 会：
1. 提醒你确认表格有**编辑权限**（右上角"分享"→确认你是"可编辑"，不是"可阅读"）
2. 自动读取你表里的字段，猜哪个字段是书名、哪个是作者、哪个是分类
3. 列一张表给你确认："我猜'书名'字段是放书名的，'标签'是分类，对吗？"
4. 你说"对"或者纠正一下
5. 打通完成

**注意**：不需要改你表里的字段名。你的字段叫"书名"就用"书名"，叫"Book Title"就用"Book Title"。

### 路径 B：我还没有表

先在飞书多维表格里新建一张表，字段参考下面这个建议结构（名字可以自己起）：

| 建议字段名 | 类型 | 说明 |
|---|---|---|
| 书名 | 文本 | 主字段 |
| 作者 | 文本 | |
| 标签/分类 | 多选 | 心理学、AI、文学……随便加 |
| 出版日期 | 日期 | 可选 |
| 简介 | 多行文本 | 可选 |
| 读完日期 | 日期 | 你什么时候读完的 |
| 状态 | 单选 | 待读 / 在读 / 已读 |
| 读书笔记 | 多行文本 | 可选 |

建完后把链接发给 agent，剩下的和路径 A 一样。

---

## 日常怎么用

打通之后，直接说人话就行：

### 加一本想读的书

> "记本书：《置身事内》"

→ agent 自动写入，状态默认是"待读"。

### 开始读了

> "我开始读《置身事内》了"

→ 状态改成"在读"。

### 读完了

> "我今天读完了《置身事内》"

→ agent 会自动搜索这本书的作者、出版年份、简介、分类，列出来给你看一眼：

```
确认写入：
书名：《置身事内》
作者：兰小欢
分类：经济
出版：2021-08
简介：复旦大学兰小欢教授讲解中国政府与经济发展的普及读物...
读完日期：2026-09-24
状态：已读

确认？
```

你说"确认"就写进去了。

### 写读后感

> "《置身事内》的感想：地方政府的土地财政逻辑比我想的更复杂，尤其是城投债那段"

→ agent 把这段话整理后写进你表里的"读书笔记"字段，追加在后面，不会覆盖之前的笔记。

### 批量录入

> "我上周五读完了《华盛顿传》，9月10号读完了《控制论》，8月20号读完了《意会》"

→ agent 一本本搜、一本本写入，全部完成后给你汇总。

---

## 注意事项

1. **编辑权限**：如果第一次写入报 403 / 没权限，去飞书表格右上角"分享"里确认你是"可编辑"
2. **选项名要精确**：分类和状态是多选/单选字段，必须从你表里已有的选项里选，agent 不会乱加新选项。如果你想加新分类，去飞书表里加好选项，下次写入就能用
3. **日期格式**：agent 自动处理，你说"上周五""9月10号"就行
4. **作者拿不准时**：如果你说了作者名，agent 会以你说的为准；没说的话 agent 会搜，搜出来给你确认
5. **笔记不会被覆盖**：每次说感想都是追加，不是替换
6. **新书自动排最上面**：主视图已配置按读完日期倒序，打开表最新的书就在第一行

---

## 常见问题

**Q：我表里有"页数""评分"这些额外字段，会怎样？**
A：初始化时 agent 会列出来问你"这个要每次填吗？"，你说不用就跳过。

**Q：我改了表结构（加了字段、改了选项）怎么办？**
A：跟 agent 说"重新配置一下我的书架表"，它会重新读字段、重新确认映射。

**Q：能在手机上用吗？**
A：能，只要你的 agent 客户端能发消息就行，写入是云端完成的。

**Q：会把我的表链接泄露吗？**
A：配置存在你本地的 `local-config.md` 里，不会上传。分享 skill 给别人时这个文件不会一起发。
